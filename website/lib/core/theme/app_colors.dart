import 'package:flutter/material.dart';

class AppColors {
  // Primary
  static const Color primary = Color(0xFF6366F1);
  static const Color primaryLight = Color(0xFF818CF8);
  static const Color primaryDark = Color(0xFF4F46E5);

  // Background
  static const Color background = Color(0xFF050A15);
  static const Color surface = Color(0xFF0F172A);
  static const Color surfaceLight = Color(0xFF1E293B);

  // Accent
  static const Color cyan = Color(0xFF00D4FF);
  static const Color green = Color(0xFF00FF88);
  static const Color pink = Color(0xFFEC4899);
  static const Color orange = Color(0xFFF59E0B);
  static const Color red = Color(0xFFEF4444);

  // Text
  static const Color textPrimary = Color(0xFFF8FAFC);
  static const Color textSecondary = Color(0xFF94A3B8);
  static const Color textMuted = Color(0xFF64748B);

  // Glass
  static const Color glassBorder = Color(0x1FFFFFFF);
  static const Color glassFill = Color(0x0DFFFFFF);
  static const Color glassFillHover = Color(0x1AFFFFFF);

  // Service gradients
  static const List<Color> foodGradient = [Color(0xFFEF4444), Color(0xFFF97316)];
  static const List<Color> taxiGradient = [Color(0xFFF59E0B), Color(0xFFFBBF24)];
  static const List<Color> courierGradient = [Color(0xFF22C55E), Color(0xFF4ADE80)];
  static const List<Color> realEstateGradient = [Color(0xFF3B82F6), Color(0xFF60A5FA)];
  static const List<Color> carSalesGradient = [Color(0xFF8B5CF6), Color(0xFFA78BFA)];
  static const List<Color> rentalGradient = [Color(0xFF14B8A6), Color(0xFF2DD4BF)];
  static const List<Color> marketGradient = [Color(0xFFEC4899), Color(0xFFF472B6)];
  static const List<Color> adminGradient = [Color(0xFF6366F1), Color(0xFF818CF8)];
}
